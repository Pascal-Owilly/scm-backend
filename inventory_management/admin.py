from django.contrib import admin
from inventory_management.models import InventoryBreed, InventoryBreedSales, BreedCut

# Registed models 

admin.site.register(InventoryBreed)
admin.site.register(InventoryBreedSales)
admin.site.register(BreedCut)
